<div class="mypage">
<h1>観光地従事者向けユーザー　マイページ　登録されていない方は編集・投稿できません。</h1>

<h3>Tourist詳細</h3>

<%= link_to "新規投稿へ", new_tourist_path %>

<%= image_tag @user.image_url, size: "250x200" if @user.image? %>

<p>名前 : <%= @user.name %></p>
<p>メールアドレス : <%= @user.email %></p>
<p>プロフィール : <%= @user.profile %></p>

<% if current_user.id == @user.id %>
  <%= link_to "編集する", edit_user_registration_path %>
   
<% end %>

<h2>ユーザーの投稿一覧</h2>
<% @user.tourists.each do |t| %>
  <%= t.user.name %>
  <%= t.user.email %>
      <%= t.name %>
      <%= t.what %>
      <%= t.how %>
      <%= t.more %>
      <%= t.access %>
<% end %>

<%= link_to "Tourist一覧に戻る", tourists_path %>
</div>
